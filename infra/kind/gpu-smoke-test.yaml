apiVersion: v1
kind: Pod
metadata:
  name: gpu-smoke-test
  namespace: kubeflow-by-doing
spec:
  restartPolicy: Never
  containers:
    - name: cuda
      image: nvidia/cuda:12.6.0-base-ubuntu24.04
      command: ["nvidia-smi"]
      resources:
        limits:
          nvidia.com/gpu: 1
