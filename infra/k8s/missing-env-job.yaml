apiVersion: batch/v1
kind: Job
metadata:
  name: missing-env-job
  namespace: kubeflow-by-doing
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: train
          image: python:3.12-slim
          command:
            - python
            - -c
            - |
              import os
              print(os.environ["DATASET_URI"])
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
